import React, { useState } from 'react';
import { PlusCircle, MapPin, Edit, X, Thermometer, Droplets, Snowflake, ShieldCheck, Sliders, Save } from 'lucide-react';

export default function ParametrosGlobais({ api, showToast, listaSetores, listaTipos, carregarParametrosGerais, carregarDadosBase, setModalConfig }) {
  const [modalParametro, setModalParametro] = useState({ isOpen: false, entidade: 'SETOR', id: '', nome: '', temp_min: '', temp_max: '', umidade_min: '', umidade_max: '', intervalo_degelo: '', duracao_degelo: '' });

  const aplicarPresetAnvisa = (e) => {
    const presetName = e.target.value; if (!presetName) return;
    const presets = {
      'Laticínios e Frios': { temp_min: 0, temp_max: 8, umidade_min: 60, umidade_max: 80, intervalo_degelo: 6, duracao_degelo: 20 },
      'Carnes Resfriadas': { temp_min: 0, temp_max: 4, umidade_min: 85, umidade_max: 95, intervalo_degelo: 4, duracao_degelo: 30 },
      'Congelados': { temp_min: -22, temp_max: -12, umidade_min: 40, umidade_max: 60, intervalo_degelo: 8, duracao_degelo: 40 },
      'Hortifruti': { temp_min: 8, temp_max: 15, umidade_min: 85, umidade_max: 95, intervalo_degelo: 12, duracao_degelo: 20 },
      'Cervejeiras': { temp_min: -4, temp_max: 2, umidade_min: 60, umidade_max: 80, intervalo_degelo: 6, duracao_degelo: 25 },
      'Vacinas e Medicamentos': { temp_min: 2, temp_max: 8, umidade_min: 40, umidade_max: 70, intervalo_degelo: 12, duracao_degelo: 20 },
      'Sorvetes': { temp_min: -25, temp_max: -18, umidade_min: 40, umidade_max: 60, intervalo_degelo: 8, duracao_degelo: 30 },
      'Pescados Resfriados': { temp_min: 0, temp_max: 2, umidade_min: 90, umidade_max: 95, intervalo_degelo: 4, duracao_degelo: 30 },
      'Chocolates e Confeitaria': { temp_min: 15, temp_max: 18, umidade_min: 40, umidade_max: 60, intervalo_degelo: 24, duracao_degelo: 15 },
      'Carnes Maturadas (Dry Aged)': { temp_min: 1, temp_max: 3, umidade_min: 75, umidade_max: 85, intervalo_degelo: 6, duracao_degelo: 20 },
      'Refeições Prontas': { temp_min: 2, temp_max: 5, umidade_min: 60, umidade_max: 80, intervalo_degelo: 8, duracao_degelo: 20 },
    };
    if (presets[presetName]) { setModalParametro(prev => ({ ...prev, nome: prev.nome || presetName, ...presets[presetName] })); showToast(`Normas ANVISA aplicadas para ${presetName}!`, 'info'); }
  };

  const salvarParametro = async (e) => {
    e.preventDefault(); const endpoint = modalParametro.entidade === 'SETOR' ? '/setores' : '/tipos-refrigeracao';
    const payload = modalParametro.entidade === 'SETOR' ? { nome: String(modalParametro.nome).trim() } : { nome: String(modalParametro.nome).trim(), temp_min: Number(modalParametro.temp_min), temp_max: Number(modalParametro.temp_max), umidade_min: Number(modalParametro.umidade_min), umidade_max: Number(modalParametro.umidade_max), intervalo_degelo: Number(modalParametro.intervalo_degelo), duracao_degelo: Number(modalParametro.duracao_degelo) };
    try {
      if (modalParametro.id) { await api.put(`${endpoint}/${modalParametro.id}`, payload); showToast(`${modalParametro.entidade === 'SETOR' ? 'Setor' : 'Tipo'} atualizado com sucesso!`, 'success'); } 
      else { if (!payload.nome) return showToast('O nome é obrigatório.', 'error'); await api.post(endpoint, payload); showToast(`${modalParametro.entidade === 'SETOR' ? 'Setor' : 'Tipo'} cadastrado com sucesso!`, 'success'); }
      setModalParametro({ isOpen: false, entidade: 'SETOR', id: '', nome: '', temp_min: '', temp_max: '', umidade_min: '', umidade_max: '', intervalo_degelo: '', duracao_degelo: '' }); carregarParametrosGerais(); carregarDadosBase(); 
    } catch(err) { const erroReal = err.response?.data?.error || err.response?.data?.message || err.message || 'Erro Desconhecido'; showToast(`Falha no Servidor: ${erroReal}`, 'error'); }
  };

  const pedirExclusaoParametro = (id, nome, entidade) => {
    const endpoint = entidade === 'SETOR' ? `/setores/${id}` : `/tipos-refrigeracao/${id}`;
    setModalConfig({ isOpen: true, title: `Remover ${entidade}`, message: `Tem a certeza que deseja remover "${nome}" permanentemente da lista?`, isPrompt: false, onConfirm: async () => { try { await api.delete(endpoint); showToast('Removido com sucesso.', 'success'); carregarParametrosGerais(); } catch (e) { showToast('Erro ao remover.', 'error'); } }});
  };

  return (
    <div className="anim-fade-in stagger-1">
      <div className="flex-header"><h3 style={{ margin: 0 }}>Parâmetros Globais</h3><div className="action-group" style={{ display: 'flex', gap: '10px' }}><button className="btn btn-primary" onClick={() => setModalParametro({ isOpen: true, entidade: 'SETOR', id: '', nome: '' })}><PlusCircle size={18}/> Novo Setor</button><button className="btn btn-info" style={{ backgroundColor: '#38bdf8', color: 'white', borderColor: '#38bdf8' }} onClick={() => setModalParametro({ isOpen: true, entidade: 'TIPO', id: '', nome: '', temp_min: 0, temp_max: 8, umidade_min: 60, umidade_max: 85, intervalo_degelo: 6, duracao_degelo: 30 })}><PlusCircle size={18}/> Novo Tipo</button></div></div>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(350px, 1fr))', gap: '1.5rem', marginTop: '1rem' }}>
        <div className="card"><h4 style={{ margin: '0 0 1rem 0', color: 'var(--text-main)', display: 'flex', alignItems: 'center', gap: '8px' }}><MapPin size={20} color="var(--primary)" /> Setores Comerciais</h4><div style={{ maxHeight: '400px', overflowY: 'auto', paddingRight: '5px' }}>{!listaSetores || listaSetores.length === 0 ? (<div className="empty-state" style={{ padding: '2rem 1rem' }}><p style={{ color: 'var(--text-muted)' }}>Nenhum setor cadastrado.</p></div>) : (<div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>{listaSetores.map(s => (<div key={s.id} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '12px 16px', backgroundColor: 'var(--bg-color)', borderRadius: '10px', border: '1px solid var(--border)' }}><strong style={{ fontSize: '1rem', color: 'var(--text-main)' }}>{s.nome}</strong><div style={{ display: 'flex', gap: '6px' }}><button className="btn btn-outline" style={{ padding: '0.4rem', border: 'none', color: 'var(--text-muted)' }} onClick={() => setModalParametro({ isOpen: true, entidade: 'SETOR', id: s.id, nome: s.nome })}><Edit size={16} /></button><button className="btn btn-outline" style={{ padding: '0.4rem', border: 'none', color: 'var(--danger)' }} onClick={() => pedirExclusaoParametro(s.id, s.nome, 'SETOR')}><X size={16} /></button></div></div>))}</div>)}</div></div>
        <div className="card"><h4 style={{ margin: '0 0 1rem 0', color: 'var(--text-main)', display: 'flex', alignItems: 'center', gap: '8px' }}><Thermometer size={20} color="var(--info)" /> Tipos de Refrigeração e Limites</h4><div style={{ maxHeight: '400px', overflowY: 'auto', paddingRight: '5px' }}>{!listaTipos || listaTipos.length === 0 ? (<div className="empty-state" style={{ padding: '2rem 1rem' }}><p style={{ color: 'var(--text-muted)' }}>Nenhum tipo cadastrado.</p></div>) : (<div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>{listaTipos.map(t => (<div key={t.id} style={{ display: 'flex', flexDirection: 'column', padding: '16px', backgroundColor: 'var(--bg-color)', borderRadius: '12px', border: '1px solid var(--border)' }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: '12px' }}><strong style={{ fontSize: '1.05rem', color: 'var(--text-main)' }}>{t.nome}</strong><div style={{ display: 'flex', gap: '6px' }}><button className="btn btn-outline" style={{ padding: '0.4rem', border: 'none', color: 'var(--text-muted)' }} onClick={() => setModalParametro({ isOpen: true, entidade: 'TIPO', id: t.id, nome: t.nome, temp_min: t.temp_min, temp_max: t.temp_max, umidade_min: t.umidade_min, umidade_max: t.umidade_max, intervalo_degelo: t.intervalo_degelo, duracao_degelo: t.duracao_degelo })}><Edit size={16} /></button><button className="btn btn-outline" style={{ padding: '0.4rem', border: 'none', color: 'var(--danger)' }} onClick={() => pedirExclusaoParametro(t.id, t.nome, 'TIPO')}><X size={16} /></button></div></div><div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px' }}><span style={{ display: 'flex', alignItems: 'center', fontSize: '0.75rem', fontWeight: '700', padding: '4px 8px', borderRadius: '6px', backgroundColor: 'rgba(16, 185, 129, 0.1)', color: 'var(--success)', border: '1px solid rgba(16, 185, 129, 0.2)' }}><Thermometer size={12} style={{ marginRight:'4px' }}/> {t.temp_min}°C a {t.temp_max}°C</span><span style={{ display: 'flex', alignItems: 'center', fontSize: '0.75rem', fontWeight: '700', padding: '4px 8px', borderRadius: '6px', backgroundColor: 'rgba(56, 189, 248, 0.1)', color: 'var(--info)', border: '1px solid rgba(56, 189, 248, 0.2)' }}><Droplets size={12} style={{ marginRight:'4px' }}/> {t.umidade_min}% a {t.umidade_max}%</span><span style={{ display: 'flex', alignItems: 'center', fontSize: '0.75rem', fontWeight: '700', padding: '4px 8px', borderRadius: '6px', backgroundColor: 'rgba(245, 158, 11, 0.1)', color: 'var(--warning)', border: '1px solid rgba(245, 158, 11, 0.2)' }}><Snowflake size={12} style={{ marginRight:'4px' }}/> Degelo: {t.duracao_degelo}m / {t.intervalo_degelo}h</span></div></div>))}</div>)}</div></div>
      </div>
      {modalParametro.isOpen && (
        <div className="modal-overlay" style={{ alignItems: 'flex-start', paddingTop: '10vh' }}><div className="modal-content" style={{ maxWidth: modalParametro.entidade === 'TIPO' ? '600px' : '400px', width: '100%' }}><h3><Sliders size={20} style={{ marginRight: '8px', verticalAlign: 'middle', color: 'var(--primary)' }}/> {modalParametro.id ? `Editar ${modalParametro.entidade === 'SETOR' ? 'Setor' : 'Tipo'}` : `Novo ${modalParametro.entidade === 'SETOR' ? 'Setor' : 'Tipo'}`}</h3><form onSubmit={salvarParametro}>{modalParametro.entidade === 'TIPO' && !modalParametro.id && (<div style={{ marginBottom: '1.5rem', padding: '12px', backgroundColor: 'rgba(56, 189, 248, 0.1)', borderRadius: '8px', border: '1px solid rgba(56, 189, 248, 0.3)' }}><label style={{ color: 'var(--text-main)', fontWeight: 'bold', fontSize: '0.85rem' }}><ShieldCheck size={16} style={{verticalAlign:'middle', marginRight: '5px', color: 'var(--info)'}}/> Preenchimento Automático</label><select className="select-input" onChange={aplicarPresetAnvisa} style={{ width: '100%', marginTop: '8px', border: '1px solid var(--info)' }}><option value="">Selecione um padrão para auto-preencher...</option><optgroup label="Alimentos Básicos"><option value="Laticínios e Frios">Laticínios e Frios (0°C a 8°C)</option><option value="Carnes Resfriadas">Carnes Resfriadas (0°C a 4°C)</option><option value="Congelados">Congelados (-22°C a -12°C)</option><option value="Hortifruti">Hortifruti / FLV (8°C a 15°C)</option><option value="Cervejeiras">Cervejeiras (-4°C a 2°C)</option><option value="Refeições Prontas">Marmitas (2°C a 5°C)</option></optgroup><optgroup label="Sensíveis (ANVISA)"><option value="Vacinas e Medicamentos">Vacinas (2°C a 8°C)</option><option value="Sorvetes">Sorvetes (-25°C a -18°C)</option><option value="Pescados Resfriados">Pescados (0°C a 2°C)</option><option value="Chocolates e Confeitaria">Chocolates (15°C a 18°C)</option><option value="Carnes Maturadas (Dry Aged)">Dry Aged (1°C a 3°C)</option></optgroup></select></div>)}<div style={{ marginBottom: '1.5rem' }}><label>Nome da Categoria</label><input type="text" value={modalParametro.nome} onChange={e => setModalParametro({...modalParametro, nome: e.target.value})} required autoFocus /></div>{modalParametro.entidade === 'TIPO' && (<><h4 style={{ margin: '1rem 0 0.5rem 0', color: 'var(--text-main)', fontSize: '0.9rem' }}>Limites Operacionais</h4><div className="form-grid" style={{ marginBottom: '1.5rem' }}><div><label>Temp. Mín (°C)</label><input type="number" step="0.1" value={modalParametro.temp_min} onChange={e => setModalParametro({...modalParametro, temp_min: e.target.value})} required /></div><div><label>Temp. Máx (°C)</label><input type="number" step="0.1" value={modalParametro.temp_max} onChange={e => setModalParametro({...modalParametro, temp_max: e.target.value})} required /></div><div><label>Hum. Mín (%)</label><input type="number" step="0.1" value={modalParametro.umidade_min} onChange={e => setModalParametro({...modalParametro, umidade_min: e.target.value})} required /></div><div><label>Hum. Máx (%)</label><input type="number" step="0.1" value={modalParametro.umidade_max} onChange={e => setModalParametro({...modalParametro, umidade_max: e.target.value})} required /></div><div><label>Ciclo Degelo (H)</label><input type="number" value={modalParametro.intervalo_degelo} onChange={e => setModalParametro({...modalParametro, intervalo_degelo: e.target.value})} required /></div><div><label>Dur. Degelo (Min)</label><input type="number" value={modalParametro.duracao_degelo} onChange={e => setModalParametro({...modalParametro, duracao_degelo: e.target.value})} required /></div></div></>)}<div className="modal-actions" style={{ marginTop: '0' }}><button type="button" className="btn btn-outline" onClick={() => setModalParametro({ ...modalParametro, isOpen: false })}>Cancelar</button><button type="submit" className="btn btn-primary"><Save size={18}/> Salvar</button></div></form></div></div>
      )}
    </div>
  );
}